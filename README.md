# DubaiRealty - Сайт агентства недвижимости 🏢

## 📋 О проекте

Профессиональный веб-сайт для агентства недвижимости в Дубае с современным дизайном и адаптивной версткой.

## 🛠️ Технологии

- **HTML5** - Семантическая разметка
- **SCSS** - Препроцессор CSS (архитектура 7-1 с @use)
- **JavaScript** - Интерактивность и функциональность
- **BEM** - Методология именования классов

## 📦 Установка

### 1. Клонировать проект
```bash
cd "/Users/macbook/Desktop/Appartments in Dubai"
```

### 2. Установить зависимости
```bash
npm install
```

## 🚀 Работа с SCSS

### Доступные команды:

```bash
# Одноразовая компиляция SCSS → CSS
npm run sass

# Автоматическая компиляция при изменении файлов (WATCH MODE)
npm run sass:watch

# Компиляция для продакшена (минифицированный CSS)
npm run sass:build
```

### Как работать с проектом:

1. **Запустите watch mode** (один раз):
   ```bash
   npm run sass:watch
   ```
   
2. **Редактируйте SCSS файлы** в папке `scss/`:
   - `scss/utils/_variables.scss` - переменные (цвета, шрифты)
   - `scss/components/` - UI компоненты
   - `scss/layout/` - структурные блоки
   
3. **CSS автоматически обновится** в файле `style.css`

4. **Откройте `index.html`** в браузере для просмотра

### ⚠️ ВАЖНО:
- **НЕ редактируйте** файл `style.css` напрямую
- Все изменения делайте только в `.scss` файлах
- SCSS автоматически скомпилируется в CSS

## 📁 Структура SCSS

```
scss/
├── main.scss              # Главный файл (импортирует все модули)
│
├── utils/                 # Утилиты
│   ├── _variables.scss   # Цвета, шрифты, брейкпоинты
│   ├── _mixins.scss      # Переиспользуемые миксины
│   ├── _functions.scss   # SCSS функции
│   └── _helpers.scss     # Вспомогательные классы
│
├── base/                  # Базовые стили
│   ├── _reset.scss       # Сброс стилей
│   ├── _base.scss        # Базовые HTML элементы
│   └── _typography.scss  # Типографика
│
├── layout/                # Структурные блоки
│   ├── _grid.scss        # Сетка и контейнеры
│   ├── _header.scss      # Шапка сайта
│   ├── _navigation.scss  # Меню навигации
│   ├── _hero.scss        # Героический блок
│   ├── _categories.scss  # Секция категорий
│   ├── _best-offers.scss # Лучшие предложения
│   ├── _content.scss     # Контентные блоки
│   ├── _contact.scss     # Форма контактов
│   └── _footer.scss      # Подвал
│
├── components/            # UI компоненты
│   ├── _logo.scss
│   ├── _button.scss
│   ├── _dropdown.scss
│   ├── _breadcrumbs.scss
│   ├── _category-card.scss
│   ├── _property-card.scss
│   ├── _form.scss
│   ├── _pagination.scss
│   ├── _carousel.scss
│   └── _language-switcher.scss
│
└── pages/                 # Стили страниц
    └── _home.scss
```

## 🎨 Работа с переменными SCSS

### Цвета (в `scss/utils/_variables.scss`):
```scss
$color-primary: #F4D03F;      // Желтый (кнопки, акценты)
$color-secondary: #2C2C2C;    // Темно-серый (фон)
$color-dark: #1A1A1A;
$color-white: #FFFFFF;
$color-gray: #808080;
```

### Использование в компонентах:
```scss
// В любом .scss файле:
@use '../utils/variables' as *;

.my-component {
    background-color: $color-primary;
    padding: $spacing-md;
}
```

## 📝 Пример работы

### 1. Изменить цвет кнопки:
Откройте `scss/components/_button.scss`:
```scss
.btn--primary {
    background-color: $color-primary; // Измените переменную
}
```

### 2. Добавить новый компонент:
1. Создайте файл `scss/components/_my-component.scss`
2. Добавьте импорт в `scss/main.scss`:
   ```scss
   @use 'components/my-component';
   ```
3. Sass автоматически скомпилирует изменения

## 🌐 Компоненты по макету

### ✅ Реализованные компоненты:

- **Header** - логотип, навигация, телефон, кнопка консультации
- **Navigation** - меню с активным состоянием и dropdown
- **Dropdown Menu** - выпадающее меню категорий
- **Breadcrumbs** - навигационная цепочка
- **Language Switcher** - переключатель EN/RU
- **Button** - кнопки с hover эффектами

### 🔜 Следующие компоненты:

- Hero Section
- Category Cards
- Property Cards
- Best Offers Grid
- Contact Form
- Footer

## 🎯 Команды разработки

```bash
# Запустить SCSS компиляцию (watch mode)
npm run sass:watch

# Открыть index.html в браузере
# (используйте Live Server в VS Code или любой другой сервер)
```

## 📞 Контакты

Dubai, UAE  
Email: dubairealty@mail.com  
Phone: +971 4 321 6789

## 📄 Дополнительная документация

- `PROJECT_STRUCTURE.md` - Детальная структура проекта
- `.gitignore` - Список игнорируемых файлов

---

**Приятной разработки! 🚀**

